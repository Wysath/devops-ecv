name: Afficher le message du commit

on:
  push:
    branches:
      - main
  workflow_dispatch:
  

jobs:
  afficher-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Afficher le message du commit
        run: echo "Le message du commit est ${{ github.event.head_commit.message }}"
